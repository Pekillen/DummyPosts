import { createTheme } from '@mui/material';


const theme = createTheme({
  palette: {
    primary: {
      main: '#133a44',
    },
    secondary: {
      main: '#b8d9d6',
    },
    grey: {
        main: '#808080',
    },
    pale: {
        main: '#f8fdfc',
    }
  }, 
})

export default theme;